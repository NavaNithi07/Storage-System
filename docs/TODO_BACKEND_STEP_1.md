Backend step 1 completed:
- Updated File schema with `category` (images/videos/documents) and indexes.

Next:
- Replace routes + controllers for category-specific upload, history, rename, and folder mapping.
