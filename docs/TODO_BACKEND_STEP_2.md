Backend step 2:
- Updated File upload endpoints to category-specific routes.
- Added history endpoint (pagination/search/category/sort).
- Added rename endpoint.

Next:
- Finish Cloudinary folder mapping verification.
- Update frontend to call new endpoints and add new UI.

